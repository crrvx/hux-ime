# SPDX-FileCopyrightText: 2026 明雅流风 <crrvx@outlook.com>
# SPDX-License-Identifier: GPL-3.0-or-later

# 键序列用例矩阵（2c 金样）。
# 格式：<名称>\t<重置选项（逗号分隔，可空）>\t<按键 repr 序列（空格分隔）>
# 重置固定：clear_composition、ascii_mode=false、tiger_sentence_early_commit=1、
# tiger_sentence_early_commit_to_preedit=0；其余选项由第二列覆盖。
# 说明：金样如实记录每步的 consumed（宿主链是否消费）；重放侧按 Consume/Forward 比对，
# 前向步骤的宿主行为（标点、光标、未锁退格等）由 K3 宿主等价物（core host）执行后比对。
idle_digit_period		5 period
letters_candidates		a b
rank_selector		a b semicolon
tab_lock		a b Tab c d
space_commit		a b space
return_commit		a b Return
escape_clear		a b Escape
tab_confirm_buffer	tiger_sentence_early_commit_to_preedit=1	a b Tab c d
tab_buffer_space	tiger_sentence_early_commit_to_preedit=1	a b Tab c space
tab_buffer_return	tiger_sentence_early_commit_to_preedit=1	a b Tab c Return
empty_code_commit		z z
# K3 ⑥：组合中编辑/导航键（librime selector/navigator/express_editor 等价物）
nav_left_right		a b Left Right Left Left Right Right
nav_home_end		a b Home End Home End
nav_backspace		a b BackSpace BackSpace BackSpace
nav_backspace_mid		a b Left Left BackSpace
nav_delete		a b Home Delete Delete Delete
nav_up_down		a b Up Up Down Down Down Down
nav_page		a b Page_Up Page_Down Page_Down Page_Up
nav_page_big		j a Down Page_Down Page_Down Page_Up Home End
nav_menu_home_end		a b Down End Home End
nav_no_menu		x x Left Right Up Down Home End Page_Up Page_Down BackSpace Delete
nav_tab_binder		a b Tab Tab Tab Tab
nav_idle_keys		Left Right Up Down Home End BackSpace Delete Page_Up Page_Down
nav_up_mid_caret		a b Left Up Down
nav_home_mid		a b Home a b
nav_mid_caret_type		a b Left Left c Delete
# K3 ⑥：缓冲态（early_commit_to_preedit=1）
tab_buffer_left	tiger_sentence_early_commit_to_preedit=1	a b Tab c Left Left
tab_buffer_home_end	tiger_sentence_early_commit_to_preedit=1	a b Tab c Home End
tab_buffer_up	tiger_sentence_early_commit_to_preedit=1	a b Tab c Up Down
tab_buffer_backspace	tiger_sentence_early_commit_to_preedit=1	a b Tab c BackSpace
tab_buffer_backspace2	tiger_sentence_early_commit_to_preedit=1	a b Tab c BackSpace BackSpace BackSpace
tab_buffer_delete	tiger_sentence_early_commit_to_preedit=1	a b Tab c Delete
# K3 ⑥：锁定态（早提交后）
tab_lock_left		a b Tab c Left Right
tab_lock_backspace		a b Tab c BackSpace BackSpace
tab_lock_delete		a b Tab c Left Delete
tab_lock_up_down		a b Tab c Up Down Page_Down
# K3 ⑦b：标点表（symbols.yaml half/full shape、pair 交替）
punct_idle_period	full_shape=0	period
punct_idle_comma	full_shape=0	comma
punct_idle_backslash	full_shape=0	backslash
punct_idle_semicolon	full_shape=0	semicolon
punct_idle_paren	full_shape=0	(
punct_pair_single	full_shape=0	apostrophe apostrophe apostrophe
punct_pair_double	full_shape=0	" "
punct_compose_period	full_shape=0	a b period
punct_compose_pair	full_shape=0	a b apostrophe apostrophe
punct_compose_comma	full_shape=0	a b comma c
punct_idle_space	full_shape=0	space
punct_full_shape_at	full_shape=1	@
punct_full_shape_minus	full_shape=1	-
punct_full_shape_space	full_shape=1	space
punct_half_shape_at	full_shape=0	@
punct_half_shape_minus	full_shape=0	-
punct_buffered_period	tiger_sentence_early_commit_to_preedit=1,full_shape=0	a b Tab c period
# 参照 abad411：菜单可见时的 ASCII 标点先确认组合（`_auto_commit` 下即上屏）再交标点表，
# 故 schema 的 key_binder `=`/`-` 翻页绑定在这条路径上被遮蔽（显式 Page_* 键不受影响）。
# 本仓判定其为上游缺陷并有意偏离：`punct_menu_equal` 在差分测试里按 DEVIATED_CASES 登记跳过
# （金样仍记录上游行为、字节不动），`punct_menu_minus` 仍逐位一致。
punct_menu_equal		j a equal
punct_menu_minus		j a minus
# K3 ⑦b：大写字母（editor char_handler：DirectCommit 先提交组合再交宿主）
upper_composing		a b A
upper_idle		A
# 宿主链 `express_editor` 的 Return 变体。
# 方案处理器只吃**未加修饰**的 `Return`/`KP_Enter`，故修饰键变体落到宿主链：
# `Ctrl+Return` = `CommitScriptText`（提交脚本文本 = preedit 去首个 \t，否则原始输入；
# 不经 `Commit()` ⇒ 不发提交通知）、`Ctrl+Shift+Return` = `CommitComment`
# （注释为空则只吞键：不清组合、不提交空串）。
editor_ctrl_return		a b Control+Return
editor_ctrl_shift_return		a b Control+Shift+Return
# 标点表的 caret 语义。参照 `Punctuator::ProcessKeyEvent` 在 caret 处
# `PushInput(ch)`，引擎按 `input[..caret] + 标点` 分段 ⇒ 提交文本只到该段末尾。
# 两条分别覆盖「无菜单」与「有菜单（处理器先确认/转发）」两种前序状态。
punct_mid_caret_comma		a b Left comma
punct_mid_caret_no_menu		x x Left comma
# selector 首页上翻。参照 `Selector::PreviousPage` 在
# `selected_index < page_size` 时 `Highlight(0)`（**归零高亮**，不是不动），并**无条件**
# 写 `paging` 标签 —— 故紧随其后的上翻页键 `-`（`when: paging`）成立。
# `nav_page_up_home_reset` 逐位覆盖「归零高亮」；`nav_page_home_minus` 记录的是**上游**
# 行为（`-` 被方案标点分支吃掉 ⇒ 提交「乙-」），本仓有意偏离上游、把该键按翻页处理，
# 故该用例在差分测试中登记为 DEVIATED_CASES（金样仍如实记录上游）。
nav_page_up_home_reset		j a Down Page_Up
nav_page_home_minus		a b Page_Up minus
# addon 数字直选（`tiger_sentence_digit_select`，**出厂缺省 true**）。
# 金样记录的是**上游**行为（方案核心无该扩展：数字作为编码字符入串），本仓按出厂缺省
# 开启 ⇒ 菜单可见时数字直选当前页候选；差异在差分测试中登记（`DEVIATED_CASES`）。
digit_menu_select		a b 1
# 分段常量 `SEGMENTATION_DELIMITER`。本仓追踪反查分支尖端
# `92a0b54` 的 schema（`delimiter: " '"`；主干 pin `abad411` 为 `" "`）⇒ `'` 之后的
# `1`/`;` 在本仓切成「abc 段 `ab'` + raw 段」、上游主干仍是单段 `ab'1`。
# ① `*_split` 两例：**比对字段实测一致**（段结构差异不在比对面：preedit 不参与比对），
#    故逐位通过、不登记；
# ② `*_page` 两例：末段类型不同 ⇒ 宿主导航键的消费不同（raw 末段不消费
#    `Up`/`Down`/`Page_*`，上游的 abc 单段消费）。金样如实记录**主干 pin** 的行为，
#    本仓行为在差分测试的 `DEVIATIONS` 里登记（同 pin 的探针实测：`PIN=92a0b54`
#    重跑同一探针即得本仓行）。
apostrophe_digit_split		a b apostrophe 1
apostrophe_semicolon_split		a b apostrophe semicolon
apostrophe_digit_page		a b apostrophe 1 Page_Down
apostrophe_semicolon_page		a b apostrophe semicolon Page_Down
